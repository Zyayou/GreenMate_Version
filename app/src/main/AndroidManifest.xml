<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 권한 추가 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application

        android:usesCleartextTraffic="true"

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GreenMate_front3"
        tools:targetApi="31">

        <!--카카오 맵 API 키 (local.properties랑 build.gradle에 숨겨놨음 )-->
        <meta-data android:name="com.kakao.sdk.AppKey" android:value=""/>

        <activity
            android:name=".IntroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MyPageActivity" android:label="MyPage 액티비티" />
        <activity android:name=".OutputAreaActivity" android:label="OutputArea 액티비티" />
        <activity android:name=".updateuserActivity" android:label="updateuser 액티비티" />
        <activity android:name=".PlacegarageActivity" android:label="Placegarage 액티비티" />

        <activity android:name=".EcosearchActivity" android:label="Ecosearch 액티비티" />
        <activity android:name=".EcodetailActivity" android:label="Ecodetail 액티비티" />
        <activity android:name=".MainpageActivity" android:label="Mainpage 액티비티" />
        <activity android:name=".QuizActivity" android:label="Quiz 액티비티" />
        <activity android:name=".RegisterActivity" android:label="join 액티비티" />

        <activity android:name=".EditActivity" android:label="edit profile 액티비티" />
        <activity android:name=".WithdrawalActivity" android:label="withdrawal 액티비티" />
        <activity android:name=".ReviewActivity" android:label="review 액티비티" />
        <activity android:name=".MainActivity" android:label="Main 액티비티" />
        <activity android:name=".LoginActivity" android:label="Login 액티비티" />
        <activity android:name=".JoinActivity" android:label="join 액티비티" />


    </application>

</manifest>